const router = require('express').Router();
const { Playlist, Song } = require('../../models');
const withAuth = require('../../utils/auth');

// TODO: Create a 'PUT' route in order to create a new song object.

// TODO: Create a 'DELETE' route in order to delete an existing song object.

module.exports = router;


